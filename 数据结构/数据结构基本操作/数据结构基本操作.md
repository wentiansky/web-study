# 数据结构基本操作

## 注意

能在 O(1)的时间复杂度完成查找的数据结构，只有`字典类型`。

## 思考顺序

1. 分析这段代码到底对数据先后进行了哪些操作；
2. 根据分析出来的数据操作，找到合理的数据结构；

## 总结

> 数据处理的基本操作只有 3 个，分别是增、删、查。

- 增和删可以细分为在数据结构中间和结尾增删，区别在于原数据的位置是否发生改变；
- 查找可以细分为按照位置和数据的值两种方式查找；
